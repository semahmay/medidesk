Flask==3.0.3
Flask-CORS==4.0.1
Flask-Limiter==3.5.0
Flask-SocketIO==5.3.6
SQLAlchemy==2.0.30
python-dotenv==1.0.1
PyJWT==2.8.0
bcrypt==4.1.3
requests==2.31.0
python-dateutil==2.9.0

# PostgreSQL driver
psycopg2-binary==2.9.9

# Redis (rate limiting + pub/sub + SocketIO)
redis==5.0.4
Flask-Limiter[redis]==3.5.0

# WebSocket worker
eventlet==0.36.1

# Object storage (S3 / MinIO)
boto3==1.34.69

# AI features (chat, medical reference, transcription)
groq==0.9.0

# Production WSGI
gunicorn==22.0.0

# Error tracking (optional — only active when SENTRY_DSN is set)
sentry-sdk[flask]==2.3.1

# Testing
pytest==8.1.1
pytest-cov==5.0.0
httpx==0.27.0
